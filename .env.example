# Frontend Build Environment Variables
# These variables are used during the build process (npm run build)

# Backend API URL (without /api suffix, as code adds /api automatically)
# Local: https://api.localhost
# Production: https://api.qoodle.top
VITE_API_URL=https://api.localhost

# Frontend Base URL
# This is used for generating Shop app links and Table QR URLs in Admin app
# Should match the backend FRONTEND_BASE_URL
# Local: https://admin.localhost
# Production: https://qoodle.top
VITE_FRONTEND_BASE_URL=https://admin.localhost
