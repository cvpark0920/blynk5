# Blynk Backend Environment Variables

# Server Configuration
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgresql://blynk:blynk@localhost:5432/blynk_db

# Redis
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production-min-32-characters
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Google OAuth 2.0
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback
# Production: https://qoodle.top/api/auth/google/callback

# Frontend Configuration
# All apps are served behind HTTPS reverse proxy:
# - https://admin.localhost/admin
# - https://{subdomain}.localhost/shop
# - https://{subdomain}.localhost/customer
# Note: For frontend build, use VITE_FRONTEND_BASE_URL in project root .env
FRONTEND_BASE_URL=https://admin.localhost
# Production: https://qoodle.top

# CORS Configuration (comma-separated list of allowed origins)
# For production, add your production domain(s)
CORS_ORIGIN=https://*.localhost
# Production: https://qoodle.top

# File Upload Configuration
UPLOAD_MAX_SIZE=5242880
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/webp

# VietQR API Configuration
# Get credentials from https://my.vietqr.io/
VIETQR_CLIENT_ID=your-vietqr-client-id
VIETQR_API_KEY=your-vietqr-api-key

# DeepL API Configuration
# Get credentials from https://www.deepl.com
DEEPL_AUTH_KEY=your-deepl-auth-key

# Web Push (VAPID) Configuration
# Generate keys: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_SUBJECT=mailto:support@qoodle.top
